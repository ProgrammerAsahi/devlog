# O-devlog

公开写作：devlog 草稿、配图、可复现脚本

这一层的约定：
- 实验代码放这里，一个任务一个子目录（`Q1.5-baseline/` 这种），别混在一起。
- 每个实验留一个 `README.md`：**怎么复现、当时的预测是多少、实测是多少**。
  这三样是他 devlog 和预测卡的原料。
- 跑基准前先确认预测卡写了（`questboard_prediction`），测完回填实测值。

任务的步骤与验收标准用 `questboard_quest` 取，不要从这里的文件里猜。
